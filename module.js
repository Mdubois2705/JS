const add = (a, b) => a + b    // function de base 

const sub = (a, b) => a - b


export {        // export vers "spread.js" destucturer avec les '{}'
    add,        // "export default add" si on veux en exporter qu'une 
    sub,
} 